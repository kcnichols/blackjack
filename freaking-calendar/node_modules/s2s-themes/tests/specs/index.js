import React from "react";
import Theme, { ApplyTheme} from '../../src/index.jsx';
//import your component here
//import Button from "../../src/index.jsx"



describe("THEME", function() {
  /* sample code here
  function afunc(){
    alert('button clicked');
  }
  before(() => {
    this.component( <Button cbClick={afunc} buttonType="reset" buttonLabel="Thomas bar" style={{buttonStyles:{backgroundColor: 'red', height: "200px", width: "200px"}}}/> );
  });
  */
  it("Reset", () => {
    const c = () => class extends Component {
      render() {
        return (<div>hello</div>);
      }
    };
   const x = ApplyTheme(c);
    this.component( <Theme appliedStyle={{"stupid": true}} >
                              <x />
                             </Theme> );
  });
  /*
  it("New Label", () => {
    console.log('hello new label clicked');
    this.props({ cbClick: afunc, buttonLabel: "a very long button label goes here button label" });
  });
  it("empty label", () => {
    console.log('hello new label clicked');
    this.props({ cbClick: afunc, buttonLabel: "" });
  });
  */
});

/*
// more sample code here
//import MyComponent from "../MyComponent.jsx";
const LOREM = "Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.";


describe("MyComponent", function() {
  let count = 0;
  this.header(`## A Simple Component`); // Markdown.

  before(() => {
    // Runs when the Suite loads.
    // Use this to load your component-under-test.
    this.component( <MyComponent /> );
  });

  it("reload", () => {
    count += 1;
    this.component( <MyComponent
                color="red"
                text={`My Component ${ count }`} /> );
  });

  section("text", () => {
    it("increment", () => {
      count += 1;
      this.props({ text: `My Component ${ count }` });
    });
    it("long", () => { this.props({ text: LOREM }) });
  });

  section("color", () => {
    it("red", () => this.props({ color: "red" }));
    it("green", () => this.props({ color: "green" }));
    it("blue", () => this.props({ color: "blue" }));
    it("orange (error)", () => this.props({ color: "orange" }));
  });
});
*/
