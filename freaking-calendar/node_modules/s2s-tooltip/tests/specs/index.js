import React from "react";
import ToolTip from 's2s-tooltip';
import { storiesOf } from '@storybook/react';
import { withKnobs, text, boolean } from '@storybook/addon-knobs';
import styled from 'styled-components';

const Top = styled(ToolTip)`
  bottom : 56px;
`;

const Left = styled(ToolTip)`
  right : 56px;
`;

const Right = styled(ToolTip)`
  left : 56px;
`;

const Bottom = styled(ToolTip)`
  top : 56px;
`;

const stories = storiesOf('ToolTip', module);
stories.addDecorator(withKnobs);

storiesOf('ToolTip', module)

  .add('Top', () => (
    <div
      className="TTContainer"
      style={{
        alignItems: 'center',
        justifyContent: 'center',
        display: 'flex',
        height: '100vh',
        width: '100vw',
        fontFamily: 'sans-serif'
      }}
    >
      <div
        className="TestRelativeComponent"
        style={{
          alignItems: 'center',
          display: 'flex',
          border: '1px solid #ccc',
          height: '48px',
          justifyContent: 'center',
          width: '48px',
          position: 'relative'
        }}
      >
        ICON AF
        <Top
          label="Tooliafasdfasdfasdo"
        />
      </div>
    </div>
  ))

  .add('Left', () => (
    <div
      style={{
        alignItems: 'center',
        justifyContent: 'center',
        display: 'flex',
        height: '100vh',
        width: '100vw',
        fontFamily: 'sans-serif'
      }}
    >
      <div
        className="TestRelativeComponent"
        style={{
          alignItems: 'center',
          display: 'flex',
          border: '1px solid #ccc',
          height: '48px',
          justifyContent: 'center',
          width: '48px',
          position: 'relative'
        }}
      >
        ICON AF
        <Left
          label="Tooliafasdfasdfasdo"
        />
      </div>
    </div>
  ))

  .add('Right', () => (
    <div
      style={{
        alignItems: 'center',
        justifyContent: 'center',
        display: 'flex',
        height: '100vh',
        width: '100vw',
        fontFamily: 'sans-serif'
      }}
    >
      <div
        className="TestRelativeComponent"
        style={{
          alignItems: 'center',
          display: 'flex',
          border: '1px solid #ccc',
          height: '48px',
          justifyContent: 'center',
          width: '48px',
          position: 'relative'
        }}
      >
        ICON AF
        <Right
          label="Tooliafasdfasdfasdo"
        />
      </div>
    </div>
  ))

  .add('Bottom', () => (
    <div
      style={{
        alignItems: 'center',
        justifyContent: 'center',
        display: 'flex',
        height: '100vh',
        width: '100vw',
        fontFamily: 'sans-serif'
      }}
    >
      <div
        className="TestRelativeComponent"
        style={{
          alignItems: 'center',
          display: 'flex',
          border: '1px solid #ccc',
          height: '48px',
          justifyContent: 'center',
          width: '48px',
          position: 'relative'
        }}
      >
        ICON AF
        <Bottom
          label="Tooliafasdfasdfasdo"
        />
      </div>
    </div>
  ))